# Example Packer variables file
# Copy to variables.pkrvars.hcl and customize

fedora_version    = "43"
registry          = "docker.io"
# registry_username is set via REGISTRY_USERNAME env var
# registry_password is set via REGISTRY_PASSWORD env var
